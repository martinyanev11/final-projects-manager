@model IEnumerable<FinalProjectManager.Data.Models.ApplicationUser>
@{
    ViewData["Title"] = "Чакащи одобрения за ръководители";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Pending Supervisor Approvals</h2>
    <a asp-action="AllSupervisors" class="btn btn-outline-secondary btn-sm">All Supervisors</a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-success">No pending approvals.</div>
}
else
{
    <table class="table table-hover">
        <thead class="table-dark">
            <tr>
                <th>Full Name</th>
                <th>Имейл</th>
                <th>Регистриран</th>
                <th class="text-end">Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.FullName</td>
                    <td>@user.Email</td>
                    <td>—</td>
                    <td class="text-end">
                        <form asp-action="Approve" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="userId" value="@user.Id" />
                            <button type="submit" class="btn btn-sm btn-success">Одобри</button>
                        </form>
                        <form asp-action="Reject" method="post" class="d-inline ms-1">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="userId" value="@user.Id" />
                            <button type="submit" class="btn btn-sm btn-danger"
                                    onclick="return confirm('Отхвърли и изтрий тази регистрация?')">Отхвърли</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
