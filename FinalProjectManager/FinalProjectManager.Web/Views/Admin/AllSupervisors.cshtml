@model IEnumerable<FinalProjectManager.Data.Models.ApplicationUser>
@{
    ViewData["Title"] = "All Supervisors";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>All Supervisors</h2>
    <a asp-action="PendingApprovals" class="btn btn-outline-warning btn-sm">Pending Approvals</a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">No supervisors registered yet.</div>
}
else
{
    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Full Name</th>
                <th>Email</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.FullName</td>
                    <td>@user.Email</td>
                    <td>
                        @if (user.IsApproved)
                        {
                            <span class="badge bg-success">Approved</span>
                        }
                        else
                        {
                            <span class="badge bg-warning text-dark">Pending</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
