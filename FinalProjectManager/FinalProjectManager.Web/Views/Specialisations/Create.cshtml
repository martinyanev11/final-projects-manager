@model FinalProjectManager.Data.Models.Specialisation
@{
    ViewData["Title"] = "Добавяне на специализация";
}

<h2>Добавяне на специализация</h2>
<hr />

<div class="row">
    <div class="col-md-5">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="Name" class="form-label">Наименование</label>
                <input asp-for="Name" class="form-control" placeholder="напр. Програмиране" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary">Създай</button>
            <a asp-action="Index" class="btn btn-secondary ms-1">Отказ</a>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
