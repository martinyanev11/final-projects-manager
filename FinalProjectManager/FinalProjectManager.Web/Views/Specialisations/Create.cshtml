@model FinalProjectManager.Data.Models.Specialisation
@{
    ViewData["Title"] = "Добави специализация";
}

<h2 class="fw-bold mb-1">Добави специализация</h2>
<hr />

<div class="row">
    <div class="col-md-5">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="Name" class="form-label">Наименование</label>
                <input asp-for="Name" class="form-control" placeholder="напр. Програмиране" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <button type="submit" class="btn text-white fw-semibold" style="background-color:#1565c0;">Създай</button>
            <a asp-action="Index" class="btn btn-outline-secondary ms-1">Отказ</a>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
