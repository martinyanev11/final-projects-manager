@model FinalProjectManager.Data.Models.Specialisation
@{
    ViewData["Title"] = "Детайли за специализация";
}

<h2>Specialisation Details</h2>
<hr />

<dl class="row">
    <dt class="col-sm-3">Име</dt>
    <dd class="col-sm-9">@Model.Name</dd>

    <dt class="col-sm-3">Ученици</dt>
    <dd class="col-sm-9">@Model.Students.Count</dd>
</dl>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Редактирай</a>
    <a asp-action="Index" class="btn btn-secondary ms-1">Обратно към списъка</a>
</div>
