@model FinalProjectManager.Web.ViewModels.StudentRegisterViewModel
@{
    ViewData["Title"] = "Регистрация на ученик";
}

<div class="row justify-content-center">
    <div class="col-md-5">
        <div class="card shadow-sm">
            <div class="card-body p-4">
                <h3 class="card-title mb-1">Регистрация на ученик</h3>
                <p class="text-muted small mb-4">Създайте акаунт, за да видите своята тема и ръководител.</p>

                <form asp-action="Register">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2"></div>

                    <div class="mb-3">
                        <label asp-for="FullName" class="form-label">Пълно име</label>
                        <input asp-for="FullName" class="form-control" autofocus />
                        <span asp-validation-for="FullName" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Email" class="form-label">Имейл</label>
                        <input asp-for="Email" class="form-control" type="email" />
                        <span asp-validation-for="Email" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="SpecialisationId" class="form-label">Специализация</label>
                        <select asp-for="SpecialisationId" asp-items="ViewBag.Specialisations" class="form-select">
                            <option value="">— Изберете специализация —</option>
                        </select>
                        <span asp-validation-for="SpecialisationId" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="ClassName" class="form-label">Клас</label>
                        <select asp-for="ClassName" asp-items="ViewBag.ClassNames" class="form-select">
                            <option value="">— Изберете клас —</option>
                        </select>
                        <span asp-validation-for="ClassName" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Password" class="form-label">Парола</label>
                        <input asp-for="Password" class="form-control" type="password" />
                        <span asp-validation-for="Password" class="text-danger small"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="ConfirmPassword" class="form-label">Потвърди паролата</label>
                        <input asp-for="ConfirmPassword" class="form-control" type="password" />
                        <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                    </div>

                    <button type="submit" class="btn btn-primary w-100">Регистрация</button>
                </form>

                <div class="text-center mt-3 small">
                    Вече имате акаунт?
                    <a asp-area="Identity" asp-page="/Account/Login">Вход</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
