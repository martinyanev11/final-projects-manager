@model FinalProjectManager.Data.Models.Supervisor
@{
    ViewData["Title"] = "Редактирай учител";
}

<h2 class="fw-bold mb-1">Редактирай учител</h2>
<hr />

<div class="row">
    <div class="col-md-5">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
            <input type="hidden" asp-for="Id" />

            <div class="mb-3">
                <label asp-for="FullName" class="form-label">Пълно Име</label>
                <input asp-for="FullName" class="form-control" />
                <span asp-validation-for="FullName" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Email" class="form-label">Имейл</label>
                <input asp-for="Email" class="form-control" type="email" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="SpecialisationId" class="form-label">Специализация</label>
                <select asp-for="SpecialisationId" asp-items="ViewBag.Specialisations" class="form-select">
                    <option value="">— Изберете специализация —</option>
                </select>
                <span asp-validation-for="SpecialisationId" class="text-danger"></span>
            </div>

            <button type="submit" class="btn text-white fw-semibold" style="background-color:#1565c0;">Запази</button>
            <a asp-action="Index" class="btn btn-outline-secondary ms-1">Отказ</a>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
